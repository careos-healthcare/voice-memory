/** Shared API payload limits — no server-only guard imports. */
export const MAX_AUDIO_BYTES = 12 * 1024 * 1024;
export const MAX_TRANSCRIPT_CHARS = 24_000;
export const MAX_ATMOSPHERE_PROMPT_CHARS = 2_000;
