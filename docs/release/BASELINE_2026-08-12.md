# Release baseline — 2026-08-12

**Purpose:** Factual snapshot before focused-beta stabilization. No product or test file changes in this baseline commit step.  
**Decision ledger:** [`FOCUSED_BETA_DECISIONS.md`](./FOCUSED_BETA_DECISIONS.md)

---

## Environment

| Item | Value | How captured |
|------|-------|--------------|
| Repository | `/Users/chiragpatel/Projects/voice-memory` | workspace |
| Git branch | `main` | `git branch --show-current` |
| Git commit | `3ac9a1f4` | `git rev-parse --short HEAD` |
| Working tree | **Dirty** — `10475` lines in `git status --short` | large in-progress tree (monorepo migration / deletions visible) |
| Flutter | `3.44.6` stable (engine `83675ed276`, framework `ee80f08bbf`) | `flutter --version` |
| Dart | `3.12.2` | `dart --version` |
| Node | `v25.2.1` | `node --version` |
| npm | `10.8.2` (packageManager in root `package.json`) | `npm --version` |
| Mobile app path | `apps/mobile/` | on-disk (docs alias `apps/voicememory_mobile/`) |
| App version | `0.2.0+48` | `apps/mobile/pubspec.yaml` |
| V1-only flag | `V1FeatureFlags.enableV1Only = true` | `lib/core/config/v1_feature_flags.dart` |

---

## Inspected contract sources

| File | Role |
|------|------|
| `package.json` (root) | npm scripts including `validate:privacy-logs`, `validate:security-aplus`, `validate:api-guard` |
| `apps/mobile/pubspec.yaml` | Flutter SDK `>=3.12.0 <4.0.0`, dependencies |
| `apps/mobile/lib/core/config/v1_launch_product_contract.dart` | Nine launch capabilities, quarantined widgets, banned claims |
| `apps/mobile/lib/core/config/v1_capability_registry.dart` | Native capability allowlist (mic, biometric, billing, notifications off, liveVoice off) |
| `apps/mobile/lib/core/config/v1_production_allowlist.dart` | Production router screens, startup phases |
| `apps/mobile/lib/router/app_router.dart` | Shell routes, quarantine redirects, `OfflineSyncVerificationScreen` route |
| `apps/mobile/lib/router/route_catalog.dart` | Canonical paths |
| `apps/mobile/LAUNCH_VALIDATION.md` | Focused release test list (16 files) |
| `apps/mobile/lib/features/release_evidence/release_evidence_pack.dart` | 18-item evidence model (code, not JSON on disk) |
| `apps/mobile/docs/RELEASE_EVIDENCE_PACK.md` | Evidence pack documentation |
| `apps/mobile/APP_STORE_SUBMISSION_PACK.md` | Submission / reviewer path (note: build number in doc `+38` vs pubspec `+48` — doc stale) |
| `apps/mobile/docs/V1_PRODUCT_CONTRACT.md` | Human-readable V1 promise and free/Pro boundaries |

---

## Commands and exact results

### 1. Flutter dependencies

```bash
cd apps/mobile && flutter pub get
```

**Result:** Exit `0`. Message: `Got dependencies!` — 93 packages have newer incompatible versions (informational per `LAUNCH_VALIDATION.md`).

---

### 2. Focused Flutter release suite

Documented in `apps/mobile/LAUNCH_VALIDATION.md`. Run from `apps/mobile`:

```bash
flutter test \
  test/ios_testflight_submission_readiness_test.dart \
  test/release_identity_consistency_test.dart \
  test/app_store_rc_polish_test.dart \
  test/mobile_production_readiness_test.dart \
  test/consumer_visible_branding_test.dart \
  test/first_run_payoff_walkthrough_test.dart \
  test/archive_home_priority_test.dart \
  test/next_evidence_plan_test.dart \
  test/archive_watchlist_test.dart \
  test/archive_depth_test.dart \
  test/archive_milestones_test.dart \
  test/shareable_archive_proof_test.dart \
  test/archive_export_pack_test.dart \
  test/launch_hardening_test.dart \
  test/revenuecat_release_config_test.dart \
  test/pro_value_packaging_test.dart
```

**Result:** Exit `1`. **269 passed, 15 failed** (~15s). Log: `/tmp/focused_release_suite.log`.

**Failing tests (complete list):**

1. `test/consumer_visible_branding_test.dart` — lib/product/consumer_ui_copy.dart has no consumer-visible VoiceMemory branding  
2. `test/consumer_visible_branding_test.dart` — lib/security/privacy_copy_policy.dart has no consumer-visible VoiceMemory branding  
3. `test/first_run_payoff_walkthrough_test.dart` — First saved entry payoff / first save card stays calm without overclaiming  
4. `test/mobile_production_readiness_test.dart` — Share-safe privacy / share-safe proof excludes transcripts…  
5. `test/first_run_payoff_walkthrough_test.dart` — Archive/Patterns ladder / 1-entry workspace is not noisy  
6. `test/first_run_payoff_walkthrough_test.dart` — Archive/Patterns ladder / 2-entry workspace shows comparison guidance…  
7. `test/first_run_payoff_walkthrough_test.dart` — First-run copy quality / first-run copy uses ArchiveMe not VoiceMemory  
8. `test/app_store_rc_polish_test.dart` — App Store release candidate / RevenueCat unconfigured copy stays calm and non-technical  
9. `test/next_evidence_plan_test.dart` — Next evidence plan privacy boundaries / archive belief screen wires plan card…  
10. `test/archive_watchlist_test.dart` — Archive watchlist privacy boundaries / archive belief screen wires watchlist card  
11. `test/archive_depth_test.dart` — Archive depth archive home wiring / archive belief screen includes depth card widget  
12. `test/archive_milestones_test.dart` — Archive milestones privacy boundaries / archive belief screen wires milestones card  
13. `test/launch_hardening_test.dart` — first two-session copy / 0-entry archive home mentions sample archive calmly  
14. `test/launch_hardening_test.dart` — first two-session copy / 1-entry next action explains why second moment matters  
15. `test/revenuecat_release_config_test.dart` — paywall shows safe message when billing not configured  

**Sample failure excerpts (for classification):**

- Branding: `forbidden "ChatGPT" in "ArchiveMe is not trying to answer better than ChatGPT…"`  
- Privacy policy: `forbidden "VoiceMemory" in "voicememory"` (`privacy_copy_policy.dart`)  
- RevenueCat: `Expected: contains 'Purchases are not available right now'` / `Actual: 'Plans are not available right now.'`  
- Archive depth wiring: `does not contain 'ArchiveDepthCard'` in `archive_belief_screen.dart`  
- Share-safe (batch): `PathNotFoundException` … `mobile_prefs.json.tmp` after test completion (async persist)  

**Isolated re-run note:** `flutter test test/mobile_production_readiness_test.dart --name "share-safe"` → exit `0`, 1 passed (2026-08-12). Batch failure retained in counts above.

---

### 3. Flutter analyze

```bash
cd apps/mobile && flutter analyze lib test
```

**Result:** Exit `0`. **6749 issues** reported (12.9s). **0 errors** (`flutter analyze lib test 2>&1 | rg "^  error •" | wc -l` → `0`). Remainder are info-level (e.g. `directives_ordering`, `eol_at_end_of_file`).

---

### 4. V1 production graph

```bash
cd apps/mobile && bash tool/validate_v1_production_graph.sh
```

**Result:** Exit `1`.

```
==> research package import guard
OK — production lib/ does not import archiveme_research
==> deferred screens must not be production route builders
==> production router screens must be allowlisted
error: OfflineSyncVerificationScreen used in router but missing from V1ProductionAllowlist.productionRouterScreens
```

---

### 5. Privacy logs validator

```bash
cd /Users/chiragpatel/Projects/voice-memory && npm run validate:privacy-logs
```

**Result:** Exit `0`. Output: `validate-privacy-logs ok`

---

### 6. Security A+ validator

```bash
npm run validate:security-aplus
```

**Result:** Exit `0`. Output: `validate-security-aplus ok`

---

### 7. API guard validator

```bash
npm run validate:api-guard
```

**Result:** Exit `1`. Static file checks in `scripts/validate-api-guard.mjs` did not print failures; subprocess failed:

```
npm run validate:grade-a-blockers-tests
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'/Users/chiragpatel/Projects/voice-memory/lib/reliability/api-guard-tests.ts'
imported from scripts/run-grade-a-blockers-tests.mjs
```

**Reason:** Expected test module path missing (likely repo layout migration — web `app/` tree deleted in working tree).

---

### 8. Supplementary (documented in LAUNCH_VALIDATION.md)

```bash
bash scripts/validate-mobile-clean-working-tree.sh
```

**Result:** Exit `0`. Output: `ok: mobile working tree has no generated journal/prefs files`

---

## Summary counts

| Check | Pass | Fail | Skip / N/A |
|-------|------|------|------------|
| Focused release suite (16 files) | 269 tests | 15 tests | — |
| `flutter analyze lib test` (errors) | 0 errors | — | 6749 info issues |
| `validate_v1_production_graph.sh` | partial (research guard) | allowlist drift | — |
| `validate:privacy-logs` | ✓ | | |
| `validate:security-aplus` | ✓ | | |
| `validate:api-guard` | static only | grade-a-blockers harness | — |
| Clean mobile working tree script | ✓ | | |

---

## Working tree constraint (this command)

Per playbook acceptance: **only documentation files added** unless release manifest work explicitly updates tooling:

- `docs/release/FOCUSED_BETA_DECISIONS.md`
- `docs/release/BASELINE_2026-08-12.md`
- `release/focused_beta_status.json` — machine-readable release truth (Command 07)

No production or test source files were modified in this step.
