# Future wedge routes (not V1 live)

V1 wedge acquisition is intentionally narrow. Only these routes are live:

- `/start/prove-enough` — primary wedge
- `/start/capacity-yes` — secondary wedge

## Future routes (gated — do not ship as live UI yet)

- `/start/work-pressure`
- `/start/rest-guilt`
- `/start/relationship-replay`

These remain documentation and test gates until first-proof and paid-intent evidence pass. Each future route must funnel into the same record → return → proof loop — not a new product surface.
