import "package:flutter/material.dart";

class ArchiveDetailDrawer extends StatelessWidget {
  const ArchiveDetailDrawer({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox();
}