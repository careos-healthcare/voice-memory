import 'package:archiveme_mobile/features/export/views/export_menu_view.dart';
import 'package:flutter/material.dart';

/// Settings export screen. The menu itself lives in [ExportMenuView].
class ExportScreen extends StatelessWidget {
  const ExportScreen({super.key});

  @override
  Widget build(BuildContext context) => const ExportMenuView();
}
