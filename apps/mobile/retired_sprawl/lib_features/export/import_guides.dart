abstract final class ImportGuides {
  ImportGuides._();

  static const dayOne =
      'In Day One, open Settings, then Import/Export, then Export JSON. '
      'Bring that file here and we will read the entries it contains.';

  static const appleNotes =
      'In Apple Notes, export the notes as JSON or CSV, then choose that file here.';
}
