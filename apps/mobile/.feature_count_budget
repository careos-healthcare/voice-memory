# Repository sprawl caps for apps/mobile. Ratchet these DOWN as cleanup lands —
# they are targets to work toward, not permanent slack. Lower a line when the
# corresponding count drops and stays down; never leave headroom "just in case".
#
# Locked to stable counts (2026-08): V1 features 14, living docs 16, root tool scripts 40.
# tool scripts: 40 includes check_null_check_operator_guards.py (null-check-operator
# guard gate covering the analyzer-excluded retired_sprawl symlinks).

max_features: 14
max_docs: 16
max_tool_scripts: 40
