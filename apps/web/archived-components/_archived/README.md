# Archived consumer web components

Component tree preserved from before the marketing-web trim. Imports use `@/archived-components/_archived/…` except live shells still in `@/components/` (`SiteHeader`, `SiteFooter`, `layout/`, `providers/`, `trust/`).
