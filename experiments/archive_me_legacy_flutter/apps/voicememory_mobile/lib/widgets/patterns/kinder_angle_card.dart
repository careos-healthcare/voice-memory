// The Patterns surface reuses the same KinderAngleCard in its compact form
// (compact: true, fromPatterns: true); this re-export keeps the patterns
// import path stable.
export '../record/kinder_angle_card.dart';
