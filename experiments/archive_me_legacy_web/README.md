# Historical consumer web client

This directory is outside the production TypeScript graph, backend release
artifact, release CI, and Flutter dependency graph. It contains the former
Next.js customer UI for historical reference only.

ArchiveMe V1 ships Flutter mobile as its only customer client. The retained
Next.js application is an API-only backend assembled by
`scripts/build-backend-release.mjs`.
