# Capacitor WebView loads this origin (must match Next.js deployment for cookies/auth).
CAPACITOR_SERVER_URL=https://voicememory.app

# Local dev on simulator (use LAN IP on physical devices):
# CAPACITOR_SERVER_URL=http://192.168.1.10:3000
