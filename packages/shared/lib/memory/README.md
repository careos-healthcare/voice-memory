# Memory modules

Internal documentation for how memory surfacing, sync, and restraint work:

- [Memory pipeline](../../docs/MEMORY_PIPELINE.md) — record → transcribe → save → reflect → notes → revisit
- [Memory surfacing guide](../../docs/MEMORY_SURFACING.md) — ranking stack, what not to build, restraint rules

Start with `lib/patterns/memory-notes.ts` for note generation and `lib/refinement/callback-suppression.ts` for shared suppression patterns.
